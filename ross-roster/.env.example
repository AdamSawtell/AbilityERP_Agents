PORT=3002
NODE_ENV=development

DB_HOST=localhost
DB_PORT=5432
DB_NAME=idempiere
DB_USER=adempiere
DB_PASSWORD=
DB_SCHEMA=adempiere
DB_POOL_MAX=10

# Required — generate a UUID for production
ROSTER_BOT_API_KEY=

# Comma-separated origins for Amplify admin UI (Phase 2). Empty = allow all in development.
CORS_ORIGINS=

AUTO_APPROVE_THRESHOLD=90
SCAN_INTERVAL_MINUTES=30
PRE_SHIFT_CONFIRM_HOURS=14
ESCALATION_HOURS_BEFORE_SHIFT=4
